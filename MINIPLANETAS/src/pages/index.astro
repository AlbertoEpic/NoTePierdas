---
import { getPlanets } from '../lib/planets';

const site = {
	title: 'MINIPLANETAS - SQLP',
	tagline: 'Micro mundos para explorar',
	lead: 'Un observatorio creativo para ideas pequenas pero intensas. Cada planeta es una historia, cada orbita una ruta.',
};
const planets = await getPlanets();
const planetCount = planets.length;
---

<html lang="es">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{site.title}</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			rel="stylesheet"
			href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,500;9..144,700&family=Space+Grotesk:wght@400;500;600&display=swap"
		/>
	</head>
	<body>
		<header class="top">
			<div class="brand">MINIPLANETAS</div>
			<nav class="top-nav">
				<a href="#explorar">Explorar</a>
				<a href="#manifiesto">Manifiesto</a>
				<a class="cta" href="#contacto">Contactar</a>
			</nav>
		</header>

		<main>
			<section class="hero" id="inicio">
				<div class="hero-text">
					<p class="eyebrow">Observatorio creativo</p>
					<h1>{site.tagline}</h1>
					<p class="lead">{site.lead}</p>
					<div class="hero-actions">
						<a class="action" href="#explorar">Entrar en la orbita</a>
						<a class="ghost" href="#manifiesto">Ver enfoque</a>
					</div>
					<div class="hero-metrics">
						<div><strong>{planetCount.toString().padStart(2, '0')}</strong><span>mundos activos</span></div>
						<div><strong>12</strong><span>rutas en estudio</span></div>
						<div><strong>28</strong><span>dias de vision</span></div>
					</div>
				</div>
				<div class="hero-orbit">
					<div class="orbit"></div>
					<div class="planet planet-a"></div>
					<div class="planet planet-b"></div>
					<div class="planet planet-c"></div>
					<div class="planet planet-d"></div>
				</div>
			</section>

			<section class="gallery" id="explorar">
				<div class="section-head">
					<h2>Cartografía de mini planetas</h2>
					<p>Colecciones experimentales para diseño, ciencia y energía creativa.</p>
				</div>
				<div class="map-layout">
					<aside class="map-panel">
						<p class="map-kicker">Mapa activo</p>
						<strong>{planetCount.toString().padStart(2, '0')} destinos trazados</strong>
						<p>
							Una lectura secuencial para navegar por tono, escala y órbita en una sola vista.
						</p>
					</aside>
					<div class="map-route">
						{planets.map((planet, index) => (
							<a
								class="route-stop"
								href={`/planetas/${planet.slug}`}
								style={`--delay: ${index * 110}ms`}
							>
								<span class="stop-index">{(index + 1).toString().padStart(2, '0')}</span>
								<div class="stop-thumb">
									<img
										src={planet.image}
										alt={`Miniatura ${planet.name}`}
										loading="lazy"
										width="74"
										height="74"
									/>
								</div>
								<div class="stop-body">
									<h3>{planet.name}</h3>
									<p>{planet.tone}</p>
									<div class="stop-meta">
										<span>{planet.orbit}</span>
										<span>{planet.size}</span>
									</div>
								</div>
								<span class="stop-go" aria-hidden="true">↗</span>
							</a>
						))}
					</div>
				</div>
			</section>

			<section class="manifesto" id="manifiesto">
				<div class="manifesto-card">
					<h2>Manifiesto SQLP</h2>
					<p>
						Diseccionar lo pequeño para revelar lo monumental. Menos ruido, más
						presencia. Cada proyecto vive con una identidad gráfica propia y un
						lenguaje de color que se mueve como un cielo vivo.
					</p>
					<div class="manifesto-actions">
						<a class="action" href="#contacto">Quiero un mini planeta</a>
						<button class="ghost" type="button">Descargar dossier</button>
					</div>
				</div>
				<div class="signal">
					<div class="signal-ring"></div>
					<div class="signal-ring ring-2"></div>
					<div class="signal-ring ring-3"></div>
				</div>
			</section>
		</main>

		<footer class="footer" id="contacto">
			<div>
				<strong>MINIPLANETAS - SQLP</strong>
				<p>Laboratorio de micro mundos visuales.</p>
			</div>
			<div class="footer-links">
				<a href="#inicio">Inicio</a>
				<a href="#explorar">Explorar</a>
				<a href="#manifiesto">Manifiesto</a>
			</div>
		</footer>
	</body>
</html>

<style>
	:global(:root) {
		--bg-1: #07040f;
		--bg-2: #0e162e;
		--bg-3: #1a0936;
		--text: #f7f2ff;
		--muted: rgba(247, 242, 255, 0.72);
		--accent: #ffb74d;
		--accent-2: #4dd0e1;
		--stroke: rgba(255, 255, 255, 0.12);
		--card: rgba(18, 12, 32, 0.78);
		--shadow: 0 24px 50px rgba(5, 7, 16, 0.45);
	}

	:global(*) {
		box-sizing: border-box;
	}

	:global(body) {
		margin: 0;
		font-family: 'Space Grotesk', sans-serif;
		color: var(--text);
		background:
			radial-gradient(circle at top right, rgba(255, 183, 77, 0.25), transparent 45%),
			radial-gradient(circle at 12% 18%, rgba(77, 208, 225, 0.2), transparent 35%),
			linear-gradient(150deg, var(--bg-2), var(--bg-1) 60%, var(--bg-3));
		min-height: 100vh;
	}

	:global(a) {
		color: inherit;
		text-decoration: none;
	}

	.top {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 1.5rem 6vw;
		position: sticky;
		top: 0;
		z-index: 10;
		background: linear-gradient(180deg, rgba(7, 4, 15, 0.92), rgba(7, 4, 15, 0.4));
		backdrop-filter: blur(14px);
		border-bottom: 1px solid rgba(255, 255, 255, 0.08);
	}

	.brand {
		font-family: 'Fraunces', serif;
		letter-spacing: 0.22em;
		font-size: 0.9rem;
		text-transform: uppercase;
	}

	.top-nav {
		display: flex;
		align-items: center;
		gap: 1.5rem;
		font-size: 0.95rem;
	}

	.top-nav .cta {
		padding: 0.55rem 1.2rem;
		border-radius: 999px;
		background: linear-gradient(120deg, var(--accent), var(--accent-2));
		color: #1b0d2b;
		font-weight: 600;
		box-shadow: 0 18px 30px rgba(255, 183, 77, 0.2);
	}

	main {
		display: grid;
		gap: 4rem;
		padding: 2rem 6vw 4rem;
	}

	.hero {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 3rem;
		align-items: center;
	}

	.hero-text {
		display: grid;
		gap: 1.5rem;
		animation: rise 0.8s ease both;
	}

	.eyebrow {
		text-transform: uppercase;
		letter-spacing: 0.34em;
		font-size: 0.7rem;
		color: var(--accent);
		margin: 0;
	}

	.hero h1 {
		font-family: 'Fraunces', serif;
		font-size: clamp(2.4rem, 3.5vw, 3.8rem);
		margin: 0;
	}

	.lead {
		color: var(--muted);
		font-size: 1.05rem;
		max-width: 34rem;
		margin: 0;
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 1rem;
	}

	.action {
		padding: 0.75rem 1.6rem;
		border-radius: 999px;
		background: linear-gradient(120deg, var(--accent), var(--accent-2));
		color: #1b0d2b;
		font-weight: 600;
		box-shadow: 0 18px 30px rgba(77, 208, 225, 0.18);
		border: none;
		cursor: pointer;
	}

	.ghost {
		padding: 0.75rem 1.6rem;
		border-radius: 999px;
		border: 1px solid var(--stroke);
		background: transparent;
		color: var(--text);
		font-weight: 500;
		cursor: pointer;
	}

	.hero-metrics {
		display: flex;
		gap: 2rem;
		flex-wrap: wrap;
		font-size: 0.9rem;
		color: var(--muted);
	}

	.hero-metrics strong {
		display: block;
		font-size: 1.4rem;
		color: var(--text);
	}

	.hero-orbit {
		position: relative;
		height: 360px;
		max-width: 360px;
		margin: 0 auto;
	}

	.orbit {
		position: absolute;
		inset: 0;
		border-radius: 50%;
		border: 1px dashed rgba(255, 255, 255, 0.12);
		animation: spin 18s linear infinite;
	}

	.planet {
		position: absolute;
		border-radius: 50%;
		filter: drop-shadow(0 18px 30px rgba(5, 7, 16, 0.6));
		animation: drift 6s ease-in-out infinite;
	}

	.planet-a {
		width: 180px;
		height: 180px;
		background: radial-gradient(circle at 30% 30%, #fff2d1, #ff8a65 60%, #6d1b7b 100%);
		top: 60px;
		left: 80px;
		animation-delay: 0.2s;
	}

	.planet-b {
		width: 70px;
		height: 70px;
		background: radial-gradient(circle at 30% 30%, #8be9fd, #4dd0e1 60%, #0f1c38 100%);
		top: 20px;
		left: 10px;
		animation-delay: 0.8s;
	}

	.planet-c {
		width: 50px;
		height: 50px;
		background: radial-gradient(circle at 30% 30%, #ffe0b2, #ffb74d 60%, #5d2c91 100%);
		bottom: 20px;
		right: 20px;
		animation-delay: 1.1s;
	}

	.planet-d {
		width: 90px;
		height: 90px;
		background: radial-gradient(circle at 30% 30%, #c5f7d8, #69f0ae 60%, #1b3d2d 100%);
		bottom: 40px;
		left: 40px;
		animation-delay: 1.4s;
	}

	.gallery {
		display: grid;
		gap: 2rem;
	}

	.section-head h2 {
		font-family: 'Fraunces', serif;
		font-size: clamp(1.8rem, 3vw, 2.4rem);
		margin: 0 0 0.5rem;
	}

	.section-head p {
		margin: 0;
		color: var(--muted);
		max-width: 32rem;
	}

	.map-layout {
		display: grid;
		grid-template-columns: minmax(220px, 280px) minmax(0, 1fr);
		gap: 1.5rem;
		align-items: start;
	}

	.map-panel {
		position: sticky;
		top: 104px;
		background: rgba(10, 14, 28, 0.82);
		border: 1px solid rgba(255, 255, 255, 0.16);
		border-radius: 20px;
		padding: 1.4rem;
		display: grid;
		gap: 0.7rem;
	}

	.map-kicker {
		margin: 0;
		text-transform: uppercase;
		letter-spacing: 0.2em;
		font-size: 0.68rem;
		color: var(--accent-2);
	}

	.map-panel strong {
		font-family: 'Fraunces', serif;
		font-size: 1.35rem;
	}

	.map-panel p {
		margin: 0;
		color: var(--muted);
		font-size: 0.95rem;
	}

	.map-route {
		display: grid;
		gap: 1rem;
		position: relative;
	}

	.map-route::before {
		content: '';
		position: absolute;
		left: 24px;
		top: 10px;
		bottom: 10px;
		width: 1px;
		background: linear-gradient(
			180deg,
			rgba(77, 208, 225, 0.45),
			rgba(255, 183, 77, 0.25),
			rgba(77, 208, 225, 0.1)
		);
	}

	.route-stop {
		position: relative;
		background: var(--card);
		border-radius: 18px;
		border: 1px solid var(--stroke);
		box-shadow: var(--shadow);
		padding: 1rem 1rem 1rem 3.1rem;
		display: grid;
		grid-template-columns: 74px minmax(0, 1fr) auto;
		gap: 1rem;
		align-items: center;
		animation: fade-in 0.8s ease both;
		animation-delay: var(--delay);
		transition: transform 0.25s ease, border-color 0.25s ease;
	}

	.route-stop:hover {
		transform: translateY(-3px);
		border-color: rgba(77, 208, 225, 0.45);
	}

	.route-stop:focus-visible {
		outline: 2px solid rgba(77, 208, 225, 0.8);
		outline-offset: 4px;
	}

	.stop-index {
		position: absolute;
		left: 0.45rem;
		top: 50%;
		transform: translateY(-50%);
		width: 28px;
		height: 28px;
		display: grid;
		place-items: center;
		font-size: 0.68rem;
		font-weight: 600;
		border-radius: 999px;
		background: rgba(77, 208, 225, 0.16);
		border: 1px solid rgba(77, 208, 225, 0.35);
		color: #b6f4ff;
	}

	.stop-thumb {
		width: 74px;
		height: 74px;
		min-width: 74px;
		min-height: 74px;
		border-radius: 50%;
		overflow: hidden;
		border: 1px solid rgba(255, 255, 255, 0.18);
		box-shadow: 0 12px 24px rgba(5, 7, 16, 0.35);
	}

	.stop-thumb img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.stop-body {
		display: grid;
		gap: 0.35rem;
	}

	.stop-body h3 {
		margin: 0;
		font-size: 1.1rem;
	}

	.stop-body p {
		margin: 0;
		color: var(--muted);
		font-size: 0.94rem;
	}

	.stop-meta {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		font-size: 0.85rem;
		color: rgba(247, 242, 255, 0.6);
	}

	.stop-go {
		font-size: 1.2rem;
		color: rgba(77, 208, 225, 0.8);
	}

	.manifesto {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
		gap: 2rem;
		align-items: center;
	}

	.manifesto-card {
		background: rgba(10, 14, 28, 0.85);
		border: 1px solid rgba(255, 255, 255, 0.16);
		border-radius: 24px;
		padding: 2rem;
		box-shadow: var(--shadow);
	}

	.manifesto-card h2 {
		font-family: 'Fraunces', serif;
		margin-top: 0;
	}

	.manifesto-card p {
		color: var(--muted);
	}

	.manifesto-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 1rem;
	}

	.signal {
		position: relative;
		height: 240px;
	}

	.signal-ring {
		position: absolute;
		border-radius: 50%;
		border: 1px solid rgba(77, 208, 225, 0.4);
		inset: 20px;
		animation: pulse 4s ease-in-out infinite;
	}

	.signal .ring-2 {
		inset: 50px;
		animation-delay: 0.8s;
	}

	.signal .ring-3 {
		inset: 80px;
		animation-delay: 1.6s;
	}

	.footer {
		border-top: 1px solid rgba(255, 255, 255, 0.1);
		padding: 2.5rem 6vw 3rem;
		display: flex;
		flex-wrap: wrap;
		gap: 2rem;
		justify-content: space-between;
		color: var(--muted);
	}

	.footer strong {
		color: var(--text);
	}

	.footer-links {
		display: flex;
		gap: 1.5rem;
	}

	@keyframes spin {
		from {
			transform: rotate(0deg);
		}
		to {
			transform: rotate(360deg);
		}
	}

	@keyframes drift {
		0%,
		100% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-12px);
		}
	}

	@keyframes pulse {
		0%,
		100% {
			opacity: 0.4;
			transform: scale(0.96);
		}
		50% {
			opacity: 0.9;
			transform: scale(1);
		}
	}

	@keyframes rise {
		from {
			opacity: 0;
			transform: translateY(14px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes fade-in {
		from {
			opacity: 0;
			transform: translateY(16px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@media (max-width: 820px) {
		.top {
			flex-direction: column;
			align-items: flex-start;
			gap: 1rem;
		}

		.top-nav {
			flex-wrap: wrap;
		}

		.hero-orbit {
			height: 300px;
		}

		.map-layout {
			grid-template-columns: 1fr;
		}

		.map-panel {
			position: static;
		}
	}

	@media (max-width: 600px) {
		.hero-actions,
		.manifesto-actions {
			flex-direction: column;
			align-items: stretch;
		}

		.footer {
			flex-direction: column;
		}

		.route-stop {
			grid-template-columns: auto 1fr;
		}

		.stop-go {
			display: none;
		}
	}
</style>
